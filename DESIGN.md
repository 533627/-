# Design System

## Direction

动态编辑式电商作战台。吸收 GreenSock Dynamic Tweens 示例的深色舞台、四色业务分区、超大粗体和磁吸回弹交互，但不照搬实验页面；后台信息结构与操作效率优先。

## Color

全局基础仍使用 daisyUI 语义色。品牌舞台色为炭黑 `#12120f`，业务强调色为米黄 `#e7e1c7`、珊瑚橙 `#f06445`、钴蓝 `#3568e8`、苔绿 `#a7b85d`；success、warning、error 只表达状态。

## Typography

采用适合中文办公环境的系统无衬线字体。工作台主标题使用流体字号 44–108px、900 字重与紧凑字距；普通页面仍保持 28–40px 标题和 14px 正文，避免牺牲密集办公场景的可读性。

## Shape and Depth

输入与按钮圆角 8px，内容容器圆角 12px。主要依靠底色层级与边框分区，避免宽泛柔和阴影和过度圆角。

## Layout

桌面端固定 288px 深色侧栏，内容最大宽度 1440px；移动端使用抽屉。工作台采用行动优先的非对称布局，列表优先于重复卡片。

## Components

导航项包含统一文字标记、名称和选中状态。任务、审批与项目均使用紧凑行列表；状态同时提供文字与语义色。空状态说明下一步可执行动作。

## Motion

桌面主要行动按钮使用指针轻量磁吸，离开后 700ms 弹性回位；卡片只做 4px 上浮。触摸设备不启用磁吸；减少动态效果时关闭所有非必要动画。
