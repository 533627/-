# Design System

## Direction

现代电商作战台，吸收 Codrops 的瑞士式强排版、非对称构图、清晰导航指示和克制微交互。冷白工作区搭配近黑导航，并使用 Logo 中的蓝紫色作为主操作色、青绿色作为辅助色。

## Color

使用 daisyUI 语义色和 OKLCH。base 系列承担主要工作表面；primary 用于当前导航和首要操作；secondary 用于强提醒；success、warning、error 只表达状态。

## Typography

采用适合中文办公环境的系统无衬线字体。页面标题 28–32px，区块标题 16–20px，正文 14px，辅助信息 12px。数字使用更强字重形成扫描锚点。

## Shape and Depth

输入与按钮圆角 8px，内容容器圆角 12px。主要依靠底色层级与边框分区，避免宽泛柔和阴影和过度圆角。

## Layout

桌面端固定 288px 深色侧栏，内容最大宽度 1440px；移动端使用抽屉。工作台采用行动优先的非对称布局，列表优先于重复卡片。

## Components

导航项包含统一文字标记、名称和选中状态。任务、审批与项目均使用紧凑行列表；状态同时提供文字与语义色。空状态说明下一步可执行动作。

## Motion

仅为悬停、焦点、抽屉和状态反馈使用 150–220ms 过渡；减少动态效果时关闭非必要过渡。
